query UserSearch{
    search(first: 10, query: "skevy", type: USER) {
        nodes {
            ... on User {
                name
                avatarUrl
                bio
                bioHTML
            }
        }
    }
}